package db;

/**
 * Offers function to manage ast type labels.
 * Uses the generated class {@code AstTypeLabelsGenerated} to translate closure compiler ast types (int) to Database ast type labels (Object)
 * and ast type labels back to integer types of the closure compiler.
 * It extends the generated class, so the generated class can be regenerated, without loosing changes here.
 */
public class AstTypeLabels extends AstTypeLabelsGenerated {

}
